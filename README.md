# Subscription Service API

REST-сервис для агрегации данных об онлайн-подписках пользователей.  
Написан на **Go**, использует **PostgreSQL**, **Gin** и **Docker**. Поддерживает полный цикл управления подписками: создание, чтение, обновление, удаление, список и расчёт общей стоимости за период.

---

## 🚀 Основные возможности

- ✅ **CRUDL** — создание, чтение, обновление, удаление и список подписок
- ✅ **Агрегация** — расчёт суммарной стоимости подписок за указанный период
- ✅ **Фильтрация** — по `user_id`, `service_name`, дате начала и окончания
- ✅ **Миграции БД** — автоматическое применение при запуске
- ✅ **Swagger-документация** — интерактивная спецификация API
- ✅ **Логирование** — все запросы и ошибки пишутся в консоль
- ✅ **Конфигурация через `.env`** — гибкая настройка окружения
- ✅ **Запуск через `docker-compose`** — одна команда для старта всего стека

---

## 📦 Технологии

| Компонент      | Версия / Использование                    |
|----------------|-------------------------------------------|
| **Go**         | 1.21+                                     |
| **Gin**        | v1.9.1 — веб-фреймворк                    |
| **PostgreSQL** | 15 — хранение данных                      |
| **Docker**     | Сборка и оркестрация                      |
| **Swagger**    | OpenAPI 3.0 — документация API            |
| **Godotenv**   | Загрузка переменных окружения из `.env`   |

---

## 🔧 Установка и запуск

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/yourname/subscription-service.git
cd subscription-service
```

### 2. Запустите сервис через Docker Compose
```bash
docker-compose up --build
```
Первый запуск может занять 1–2 минуты: скачается образ PostgreSQL, применится миграция, соберётся Go-приложение.

---

## 🌐 Доступные эндпоинты

Основной URL
http://localhost:8080/api/v1

### 1. Создать подписку — POST /subscriptions
Пример тела запроса:

```json
{
  "service_name": "Yandex Plus",
  "price": 400,
  "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
  "start_date": "07-2025"
}
```
Поле end_date — опционально. Формат даты: MM-YYYY.

### 2. Получить подписку по ID — GET /subscriptions/:id
Пример:

```bash
curl http://localhost:8080/api/v1/subscriptions/1
```

### 3. Обновить подписку — PUT /subscriptions/:id
Тело запроса аналогично POST, но все поля необязательны — обновляются только переданные.

### 4. Удалить подписку — DELETE /subscriptions/:id
Возвращает статус 204 No Content.

### 5. Список подписок — GET /subscriptions
Поддерживает фильтрацию через query-параметры:

| Параметр          | Назначение                                          |
|-------------------|-----------------------------------------------------|
| user_id           | Фильтр по ID пользователя                           |
| service_name      | Фильтр по названию сервиса                          | 
| start_after       | Подписки, начавшиеся после даты (в формате MM-YYYY) | 
| end_before        | Подписки, закончившиеся до даты (в формате MM-YYYY) |



Пример запроса:

```bash
curl "http://localhost:8080/api/v1/subscriptions?user_id=60601fee-2bf1-4721-ae6f-7636e79a0cba&service_name=Yandex%20Plus"
```
### 6. Расчёт общей стоимости — GET /calculate
Рассчитывает суммарную стоимость всех активных подписок за указанный период. Учитываются только те подписки, которые пересекаются с периодом.

Параметры:

user_id — ID пользователя
service_name — название сервиса
period_start — начало периода (MM-YYYY)
period_end — конец периода (MM-YYYY)

Пример:

```bash
curl "http://localhost:8080/api/v1/calculate?user_id=60601fee-2bf1-4721-ae6f-7636e79a0cba&service_name=Yandex%20Plus&period_start=01-2025&period_end=12-2025"
```

Пример ответа:

```json
{
  "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
  "service_name": "Yandex Plus",
  "period_start": "01-2025",
  "period_end": "12-2025",
  "total_cost": 4800
}
```

Подсчёт учитывает только те подписки, которые были активны в указанном периоде (включая бессрочные с end_date = `null`).

---

## 📄 Swagger-документация
Интерактивная документация доступна через Swagger UI:

### 👉 Перейдите по ссылке:
http://localhost:8080/swagger

Вы можете:

Просматривать все эндпоинты
Проверять форматы запросов и ответов
Отправлять тестовые запросы прямо из браузера

## ⚙️ Конфигурация
Настройки задаются в файле .env или через переменные окружения.

| Переменная       | Описание                     | По умолчанию        | 
|------------------|------------------------------|---------------------|
| SERVER_PORT      | Порт HTTP-сервера            | 8080                |
| DB_HOST          | Хост PostgreSQL              | db (в Docker)       |
| DB_PORT          | Порт PostgreSQL              | 5432                |
| DB_USER          | Пользователь БД              | postgres            |
| DB_PASSWORD      | Пароль пользователя          | postgres            |
| DB_NAME          | Имя базы данных              | subscriptions_db    |
| DB_SSLMODE       | Режим SSL подключения        | disable             |



В docker-compose.yml имя сервиса базы — db, поэтому в контейнере Go-приложение использует db как хост.

---

## 🧹 Логирование
Сервис логирует:

Все входящие HTTP-запросы (через Gin)
Ошибки подключения к БД
Операции с подписками

Пример лога:
```
[GIN] 2025/04/05 - 12:34:56 | 201 |     120ms | 172.20.0.1 | POST     "/api/v1/subscriptions"
Создана подписка: ID=1, Service=Yandex Plus, User=60601fee-2bf1-4721-ae6f-7636e79a0cba
```

---

## 🙌 Автор

enritexgo